CREATE TABLE usuario(ID INT PRIMARY KEY AUTO_INCREMENT,NOMRBE VARCHAR(150),EDAD INT(100), CORREO VARCHAR(250));
CREATE TABLE curso(ID_CURSO INT PRIMARY KEY AUTO_INCREMENT,NOMRBE VARCHAR(150),ID INT, FOREIGN KEY(ID) REFERENCES usuario(ID));